import styled from 'styled-components';
import { ON_LIGHT_TEXT_COLOR_SECONDARY } from '../../../constants/theme';

const H1 = styled.h1`
  color: ${ON_LIGHT_TEXT_COLOR_SECONDARY}
`;

export default H1;
